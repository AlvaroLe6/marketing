export const validationSchema = {
 /*nombre(value) {
    if (value?.length >= 3) return true;
    return 'El nombre es obligatorio o es muy corto';
  },
  apellido(value) {
    if (value?.length >= 3) return true;
    return 'El apellido es obligatorio o es muy corto';
  },
  nroCarnet(value) {
    const pattern = /^[0-9]*$/;
    if (pattern.test(value) && value.length > 0) return true;
    return 'El número de carnet es obligatorio y solo debe contener números';
  },
  genero(value) {
    if (value) return true;
    return 'El género es obligatorio';
  },
  fechaNacimiento(value) {
    if (value) return true;
    return 'La fecha de nacimiento es obligatoria';
  },
  gradoAcademico(value) {
    if (value) return true;
    return 'El grado académico es obligatorio';
  },
  profesion(value) {
    if (value) return true;
    return 'La profesión es obligatoria';
  },
  ciudadResidencia(value) {
    if (value) return true;
    return 'La ciudad de residencia es obligatoria';
  },
  institucionEgreso(value) {
    if (value) return true;
    return 'La institución de egreso es obligatoria';
  },
  asesor(value) {
    if (value) return true;
    return 'El asesor es obligatorio';
  },
  domicilio(value) {
    if (value) return true;
    return 'El domicilio es obligatorio';
  },
  celular(value) {
    const pattern = /^[0-9]*$/;
    if (pattern.test(value) && value.length > 0) return true;
    return 'El celular es obligatorio y solo debe contener números';
  },
  correo(value) {
    const pattern = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;
    if (pattern.test(value)) return true;
    return 'Introduce un correo válido';
  },*/
};
